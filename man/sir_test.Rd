% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sir_test.R
\name{sir_test}
\alias{sir_test}
\title{Synergy via Isotonic Regression (SIR) Test}
\usage{
sir_test(
  matrix,
  weights = NULL,
  B = 200,
  direction = c("decreasing", "increasing"),
  synergy_sign = c("negative", "positive"),
  stat = "S2",
  residual_scale = c("none", "df", "hc2"),
  osqp_pars = list(),
  seed = 1L,
  n_cores = 1L,
  use_parallel = FALSE,
  threshold = 0,
  ...
)
}
\arguments{
\item{matrix}{Numeric matrix of mean responses on a transformed scale,
with rows indexed by doses of drug A and columns by doses of drug B.
Rows and columns should be ordered by increasing dose.}

\item{weights}{Numeric matrix of nonnegative weights of the same
dimensions as \code{matrix}. Typical use is the inverse-variance weight
\code{m_ij / s2_ij} produced by replicate-aggregation helpers. If
\code{NULL}, all finite cells receive weight 1.}

\item{B}{Integer number of wild-bootstrap resamples. Defaults to 200.}

\item{direction}{Direction of monotonicity in dose: \code{"decreasing"} for
viability data (default), \code{"increasing"} for inhibition data.}

\item{synergy_sign}{Direction of synergy (signed sense of \code{delta}):
\code{"negative"} (default) when more synergy means a more negative
\code{delta}, as is conventional for viability data; \code{"positive"} for
inhibition data.}

\item{stat}{Test statistic used for the bootstrap p-value. See
\code{\link[=wild_bootstrap]{wild_bootstrap()}} for allowed values. Defaults to \code{"S2"}.}

\item{residual_scale}{Residual scaling used inside the wild bootstrap.
See \code{\link[=wild_bootstrap]{wild_bootstrap()}}. Defaults to \code{"none"}.}

\item{osqp_pars}{Named list of OSQP solver parameters (see
\code{\link[osqp:osqpSettings]{osqp::osqpSettings()}}).}

\item{seed}{Integer seed for the bootstrap.}

\item{n_cores}{Integer number of worker processes to use for the
bootstrap. \code{1} (default) runs serially.}

\item{use_parallel}{Logical: when \code{TRUE} and \code{n_cores > 1}, the
bootstrap is parallelised via the future framework.}

\item{threshold}{Threshold for the \code{area_synergy} statistic.}

\item{...}{Additional arguments forwarded to \code{\link[=wild_bootstrap]{wild_bootstrap()}}.}
}
\value{
An object of class \code{"sir_test"}: a list with components
\code{delta} (interaction surface, isotonic minus additive),
\code{theta_iso} (fully monotone fit), \code{theta_add} (monotone-additive
fit), \code{S2} (interaction energy), \code{p_value} (wild-bootstrap p-value),
\code{t0} (observed test statistic), \code{bootstrap_samples} (vector of
bootstrap statistics), \code{direction}, \code{stat}, \code{synergy_sign},
and \code{B}.
}
\description{
Fits a monotone-additive null and a fully monotone two-dimensional
isotonic surface to a dose-response matrix, computes the interaction
surface as their difference, and returns a wild-bootstrap p-value
against the additive null.
}
\examples{
set.seed(1)
I <- 6; J <- 6
u <- -cumsum(abs(rnorm(I, 0.3, 0.1))); u <- u - mean(u)
v <- -cumsum(abs(rnorm(J, 0.3, 0.1))); v <- v - mean(v)
Z_add <- outer(u, v, "+")
Z <- Z_add + 0.05 * matrix(rnorm(I * J), I, J)
fit <- sir_test(Z, B = 50, direction = "decreasing")
fit$S2
fit$p_value

}
\seealso{
\code{\link[=additive_ordered_fit]{additive_ordered_fit()}}, \code{\link[=isotonic_2d_fit]{isotonic_2d_fit()}},
\code{\link[=wild_bootstrap]{wild_bootstrap()}}, \code{\link[=interaction_summaries]{interaction_summaries()}}.
}
