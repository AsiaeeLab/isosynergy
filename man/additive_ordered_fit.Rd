% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/additive_ordered.R
\name{additive_ordered_fit}
\alias{additive_ordered_fit}
\title{Monotone-Additive Surface Fit}
\usage{
additive_ordered_fit(
  barZ,
  w,
  direction = c("decreasing", "increasing"),
  osqp_pars = list(),
  ridge = 1e-09
)
}
\arguments{
\item{barZ}{Numeric matrix of (transformed) mean responses; rows
indexed by doses of drug A, columns by doses of drug B.}

\item{w}{Numeric matrix of nonnegative weights, same dimensions as
\code{barZ}.}

\item{direction}{Direction of monotonicity in dose: \code{"decreasing"}
(default) for viability data, \code{"increasing"} for inhibition data.}

\item{osqp_pars}{Named list of OSQP solver parameters (see
\code{\link[osqp:osqpSettings]{osqp::osqpSettings()}}).}

\item{ridge}{Small ridge added to the diagonal of the QP for numerical
stability.}
}
\value{
A list with components \code{theta} (the fitted I-by-J surface),
\code{alpha} (intercept), \code{u} (length-I row effects), \code{v} (length-J
column effects), \code{status} (OSQP solver status), and \code{objective}
(final objective value).
}
\description{
Fits the monotone-additive null surface
\eqn{\theta_{ij} = \alpha + u_i + v_j}{theta_ij = alpha + u_i + v_j}
to a weighted dose-response matrix, with \code{u} and \code{v} constrained to be
monotone in the requested direction and to sum to zero (for
identifiability). Solved as a sparse quadratic program via
\code{\link[osqp:osqp]{osqp::osqp()}}.
}
\seealso{
\code{\link[=isotonic_2d_fit]{isotonic_2d_fit()}}, \code{\link[=interaction_fit]{interaction_fit()}}, \code{\link[=sir_test]{sir_test()}}.
}
