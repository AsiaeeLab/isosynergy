% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{wild_bootstrap}
\alias{wild_bootstrap}
\title{Wild Bootstrap p-Value for Drug Synergy Testing}
\usage{
wild_bootstrap(
  barZ,
  w,
  direction,
  B,
  seed = 1,
  osqp_pars = list(),
  n_cores = 1,
  use_parallel = TRUE,
  stat = c("t_int", "t_int_norm", "S2", "S2_norm", "Splus", "Sminus", "max_synergy",
    "S2_plus", "S2_minus", "area_synergy", "area_synergy_weighted"),
  synergy_sign = c("negative", "positive"),
  threshold = 0,
  residual_scale = c("none", "df", "hc2"),
  df_null = NULL,
  df_tol = 1e-08
)
}
\arguments{
\item{barZ}{Numeric matrix of (transformed) mean responses.}

\item{w}{Numeric matrix of nonnegative weights, same dimensions as
\code{barZ}.}

\item{direction}{Direction of monotonicity (\code{"decreasing"} or
\code{"increasing"}).}

\item{B}{Integer number of wild-bootstrap resamples.}

\item{seed}{Integer seed used for reproducible bootstrap draws.}

\item{osqp_pars}{Named list of OSQP solver parameters.}

\item{n_cores}{Integer number of parallel workers.}

\item{use_parallel}{Logical: when \code{TRUE} and \code{n_cores > 1}, parallelise
the bootstrap via \code{\link[future:plan]{future::plan()}}/\code{\link[future.apply:future_lapply]{future.apply::future_sapply()}}.}

\item{stat}{Test statistic. Options include \code{"t_int"} (default),
\code{"t_int_norm"}, \code{"S2"}, \code{"S2_norm"}, \code{"Splus"}, \code{"Sminus"},
\code{"S2_plus"}, \code{"S2_minus"}, \code{"max_synergy"}, \code{"area_synergy"},
\code{"area_synergy_weighted"}.}

\item{synergy_sign}{Direction of synergy: \code{"negative"} or \code{"positive"}.}

\item{threshold}{Threshold for \code{area_synergy} statistics.}

\item{residual_scale}{Residual scaling: \code{"none"} (default), \code{"df"}
(degrees-of-freedom correction based on distinct runs in the
monotone-additive fit), or \code{"hc2"} (heteroskedasticity-consistent).}

\item{df_null}{Optional manual override for the degrees-of-freedom
used by \code{residual_scale = "df"}.}

\item{df_tol}{Tolerance used to count distinct runs when computing
the data-driven \code{df_null}.}
}
\value{
A list with components \code{t0} (observed statistic), \code{t_star}
(vector of bootstrap statistics, length \code{B}), \code{p_value} (one-sided
bootstrap p-value), \code{fit_add}, \code{fit_iso}, and the inputs
\code{stat}, \code{synergy_sign}, \code{threshold}, \code{residual_scale}, \code{df_null}.
}
\description{
Refits the monotone-additive null and the fully monotone isotonic
surface, computes residuals from the null fit, and resamples them
using Rademacher wild-bootstrap weights to build the null distribution
of the chosen test statistic. Returns the observed statistic, the
bootstrap reference distribution, and the resulting p-value.
}
\seealso{
\code{\link[=sir_test]{sir_test()}}, \code{\link[=additive_ordered_fit]{additive_ordered_fit()}}, \code{\link[=isotonic_2d_fit]{isotonic_2d_fit()}},
\code{\link[=interaction_fit]{interaction_fit()}}.
}
